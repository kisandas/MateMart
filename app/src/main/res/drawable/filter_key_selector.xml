<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_checked="true" android:drawable="@drawable/white_drawable" />
   <!-- Default state -->
    <item android:drawable="@drawable/default_background" />
</selector>